一.CDN是什么？为什么要用CDN?
1.CDN指的是一组分布在各个地区的服务器
2.缓存和本地存储带来的性能提升都是在获取资源并且将其存储起来之后才能做，但是首次请求资源时，这些是无效的，
那我们可以借助cdn来提高第一次响应的速度。

二.CDN的核心功能特写
1.缓存
  缓存就是我们把资源copy一份到CDN服务器的过程
2.回源
  回源就是CDN服务器发现自己没有这个资源（一般是过期），从而向它的根服务器（或者上层服务器）去要这个资源的过程

  